# Environment secrets — NEVER commit these
.env
.env.local
.env.production

# Test artifacts
test-results/
playwright-report/

# Vite cache
.vitecache/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated lint/test reports — run these locally, don't commit output
eslint-output*.txt
eslint-report*.json
eslint-compact.txt
lint-check.txt

# Binary test tools — install locally, not tracked in git
*.exe
k6.exe

# Alternate lockfiles — this project uses npm (package-lock.json)
pnpm-lock.yaml
yarn.lock
