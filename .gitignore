# Dependencies
node_modules/

# Environment variables (configure in Hostinger hPanel)
.env
.env.local
.env.*.local

# Build output (generated by: npm run build)
dist/

# Runtime data files — generated automatically by the server; never commit these
admin-config.json
acceptance-log.json
activity-log.json
analytics-data.json
comments.json
leads.json
newsletter.json
referrals.json
reviews-pending.json
uploads/

# Replit internals — not relevant for Hostinger
.vitecache/
.cache/
.local/
.agents/
.replit
replit.md

# OS clutter
.DS_Store
Thumbs.db
